
language: python

python:

  - "3.6-dev"

install:
    - pip install -r requirements.txt
    - pip install pytest pytest-cov
    - pip install coveralls
    - coverage run --source=mypkg setup.py test
    


script:
  - pytest --cov=test_endpoints.py/

 after-success:
  - coveralls



