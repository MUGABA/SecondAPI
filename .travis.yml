
language: python

python:

  - "3.6.3s"

install:
    - pip install -r requirements.txt
    - pip install pytest pytest-cov
    - pip install coveralls


script:
  - pytest --cov=./

after-success:
  - coveralls



