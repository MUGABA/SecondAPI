
language: python

python:

  - "3.7"

install:
    - pip install -r requirements.txt
    - pip install pytest pytest-cov
    - pip install coveralls


script:
  - pytest --cov=./

after-success:
  - coveralls

matrix:
  include:
    - python 3.7
    dist: xenial
    sudo: true

